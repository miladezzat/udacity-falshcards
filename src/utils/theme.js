import { DefaultTheme, Colors } from "react-native-paper";

export const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: "#2419be",
    secondary: Colors.purple500,
    error: Colors.red500,
    font:"#fff"
  }
};
